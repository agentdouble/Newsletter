[project]
name = "anjanews-backend"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "alembic>=1.13.2",
  "fastapi>=0.111.0",
  "openai>=2.14.0",
  "psycopg[binary]>=3.2.0",
  "sqlalchemy>=2.0.30",
  "uvicorn>=0.30.0",
]

[tool.uv]
dev-dependencies = []
